# StockStream V3.0 环境变量配置模板
# 复制为 .env 并根据需要修改: cp .env.example .env
# 环境变量优先级: .env > docker-compose.yml default > 代码内默认值

# ================================================================
# 核心配置
# ================================================================
STOCKSTREAM_ENV=production
STOCKSTREAM_PORT=8080
STOCKSTREAM_TAG=latest
LOG_LEVEL=info
TZ=Asia/Shanghai

# ================================================================
# 数据库
# ================================================================
# SQLite (默认, 嵌入式, 无需额外服务)
# 生产环境可切换为 PostgreSQL: docker compose --profile full up -d
POSTGRES_USER=stockstream
POSTGRES_PASSWORD=changeme
POSTGRES_DB=stockstream
POSTGRES_PORT=5432

# ================================================================
# Redis 缓存 (自动启动)
# ================================================================
REDIS_URL=redis://redis:6379/0

# ================================================================
# AI 分析
# ================================================================
DEEPSEEK_API_KEY=your_api_key_here
DEEPSEEK_MODEL=deepseek-chat

# ================================================================
# TTS 语音合成
# ================================================================
# Piper TTS 中文模型: zh_CN-huayan-medium
TTS_VOICE=zh_CN-huayan-medium

# ================================================================
# 推流 (RTMP)
# ================================================================
RTMP_URL=rtmp://your-rtmp-server/live/stream

# ================================================================
# 直播平台 (弹幕/互动抓取)
# ================================================================
DOUYIN_COOKIE=
DOUYIN_ROOM_ID=

# ================================================================
# 监 控 (profile: monitoring)
# ================================================================
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# ================================================================
# V3.0 资源管理 (Jetson 自动模式无需手动设置)
# ================================================================
# STOCKSTREAM_RESOURCE__MEMORY_THRESHOLD=0.80
# STOCKSTREAM_RESOURCE__GPU_THRESHOLD=0.90
# STOCKSTREAM_RESOURCE__CHECK_INTERVAL=30
