# StockStream V3.0 — Jetson Xavier NX Python 3.9 Production Requirements
#
# 目标平台:  Jetson Xavier NX 8GB / JetPack 5.1.x / Ubuntu 20.04 / Python 3.9
# 架构:      ARM64 / CUDA 11.4 / TensorRT 8.x / GPU Volta
#
# 版本策略:
#   - 基础 Python 版本: 3.9 (deadsnakes PPA)
#   - 严格遵守 requires-python >= 3.9 的版本约束
#   - 优先选择已验证的稳定版本
#
# 安装:
#   pip install -r requirements-jetson.txt

# ================================================================
# Web 框架 (FastAPI + ASGI)
# ================================================================
fastapi==0.115.6
uvicorn[standard]==0.27.1
pydantic==2.5.3
pydantic-settings==2.2.1
starlette==0.37.2

# ================================================================
# 数据库 / 缓存
# ================================================================
SQLAlchemy==2.0.36
aiosqlite==0.20.0
redis==5.0.1

# ================================================================
# HTTP / WebSocket 通信
# ================================================================
httpx==0.27.2
websockets==12.0
websocket-client==1.6.4
requests==2.31.0

# ================================================================
# 环境 / 配置
# ================================================================
python-dotenv==1.0.1
pyyaml==6.0.1

# ================================================================
# 数值计算 / 数据处理 (Python 3.9 版本上限放宽)
# ================================================================
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<2.3.0
scipy>=1.10.0,<1.14.0

# ================================================================
# 金融数据 (akshare 现最低版本 1.16.72, 要求 Python >=3.9)
# ================================================================
akshare>=1.16.72,<2.0.0
tushare>=1.4.1
zzshare>=0.4.1   # 0.4.0 不存在, 最新 0.4.8

# ================================================================
# 图表渲染
# ================================================================
matplotlib>=3.7.0,<3.10.0
plotly==5.17.0
Pillow>=9.5.0,<11.0.0

# ================================================================
# 头像 — Wav2Lip ONNX 推理
# ================================================================
# onnxruntime CPU 版 (PyPI 提供 aarch64 wheel)
# GPU 加速通过 JetPack 自带 CUDA/TensorRT 库 + onnxruntime-gpu 源码编译
onnxruntime==1.16.3
opencv-python-headless==4.9.0.80
librosa==0.9.2
soundfile==0.12.1

# ================================================================
# 音频 / TTS
# ================================================================
# piper-tts==1.2.0  # 需验证 Python 3.9 + ARM64 兼容性

# ================================================================
# 调度 / 定时任务
# ================================================================
APScheduler==3.10.4

# ================================================================
# 工具类
# ================================================================
Jinja2==3.1.2
aiohttp==3.8.6
click==8.1.7

# ================================================================
# Jetson GPU 推理 (单独处理)
# ================================================================
# onnxruntime-gpu 在 PyPI 上无 aarch64 wheel.
# CPU 版 onnxruntime==1.16.3 已安装, 对 Wav2Lip 推理够用.
# 如需 GPU 加速:
#   docker build 时通过 --build-arg 传入本地预编译的 .whl
#   或从 https://github.com/microsoft/onnxruntime 源码编译:
#   ./build.sh --config Release --build_wheel --use_cuda --cuda_home=/usr/local/cuda
#              --use_tensorrt --tensorrt_home=/usr/src/tensorrt --parallel
