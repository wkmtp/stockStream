# StockStream V3.0 — Jetson Xavier NX Python 3.8 Production Requirements
#
# 目标平台:  Jetson Xavier NX 8GB / JetPack 5.1.x / Ubuntu 20.04 / Python 3.8.10
# 架构:      ARM64 / CUDA 11.4 / TensorRT 8.x / GPU Volta
#
# 版本策略:
#   - 所有包严格锁定支持 Python 3.8 的最新版本
#   - 优先选择 NVIDIA 官方或社区验证的 Jetson ARM64 版本
#   - 禁止使用 requires-python >= 3.9 的版本
#
# 安装:
#   pip install -r requirements-jetson.txt
#   pip install onnxruntime-gpu==1.16.3    # Jetson GPU 推理

# ================================================================
# Web 框架 (FastAPI + ASGI)
# ================================================================
fastapi==0.115.6          # 全版本支持 Python 3.8+
uvicorn[standard]==0.27.1 # 0.28+ 可能需 3.9
pydantic==2.5.3           # 2.5.x 最稳定支持 3.8
pydantic-settings==2.2.1
starlette==0.37.2         # fastapi 0.115.6 的对等依赖

# ================================================================
# 数据库 / 缓存
# ================================================================
SQLAlchemy==2.0.36        # 2.0.x 全系支持 3.8
aiosqlite==0.20.0
redis==5.0.1              # 可选: 高性能缓存

# ================================================================
# HTTP / WebSocket 通信
# ================================================================
httpx==0.27.2             # 0.28+ 需 3.9
websockets==12.0          # 13+ 需 3.9
websocket-client==1.6.4   # 备用同步 WS 客户端
requests==2.31.0          # HTTP 同步客户端

# ================================================================
# 环境 / 配置
# ================================================================
python-dotenv==1.0.1
pyyaml==6.0.1             # YML 配置解析

# ================================================================
# 数值计算 / 数据处理
# ================================================================
numpy==1.24.4             # 1.24.4 = 支持 3.8 的最高版本
pandas==2.0.3             # 2.0.3 = 支持 3.8 的最高版本
scipy==1.10.1             # 1.10.x = 支持 3.8 的最高版本

# ================================================================
# 金融数据
# ================================================================
akshare==1.12.21          # 1.12.x 系支持 3.8
tushare==1.4.0
zzshare==0.4.0

# ================================================================
# 图表渲染
# ================================================================
matplotlib==3.7.5         # 3.7.5 = 支持 3.8 的最高版本
plotly==5.17.0            # 5.17.x 支持 3.8
Pillow==9.5.0             # 9.5.0 = 支持 3.8; 10.x 需 3.9

# ================================================================
# 头像 — Wav2Lip ONNX 推理
# ================================================================
onnxruntime==1.16.3       # 支持 Python 3.7-3.11
# onnxruntime-gpu==1.16.3 # Jetson 专用, 单独安装
opencv-python-headless==4.9.0.80   # 4.9.x 支持 3.8 ARM64
librosa==0.9.2            # 0.9.2 支持 3.8; 0.10+ 需 3.9
soundfile==0.12.1

# ================================================================
# 音频 / TTS
# ================================================================
# piper-tts==1.2.0        # 需验证 Python 3.8 + ARM64 兼容性
# 备选: 使用 ONNX 版 Piper 模型 + onnxruntime 直接推理

# ================================================================
# 调度 / 定时任务
# ================================================================
APScheduler==3.10.4       # 3.10.x 支持 3.8

# ================================================================
# 工具类
# ================================================================
Jinja2==3.1.2             # 模板引擎 (仪表盘 HTML)
aiohttp==3.8.6            # 异步 HTTP 客户端
click==8.1.7              # CLI

# ================================================================
# Jetson 专用 (单独安装)
# ================================================================
# onnxruntime-gpu==1.16.3   # CUDA 11.4 / TensorRT 8.5 EP
# 安装: pip install onnxruntime-gpu==1.16.3
#
# 注意:
#   - JetPack 5.1.x 已包含 CUDA 11.4, cuDNN 8.6, TensorRT 8.5
#   - onnxruntime-gpu 会自动检测并使用 CUDA/TensorRT EP
#   - 如 GPU 不可用将自动回退到 CPU EP (不会崩溃)

# ================================================================
# 已排除的依赖 (不兼容 Python 3.8)
# ================================================================
# numpy >= 1.25       — Requires Python >= 3.9
# pandas >= 2.1       — Requires Python >= 3.9
# matplotlib >= 3.8   — Requires Python >= 3.9
# websockets >= 13    — Requires Python >= 3.9
# httpx >= 0.28       — Requires Python >= 3.9
# Pillow >= 10.0      — Requires Python >= 3.9
# onnxruntime >= 2.0  — Requires Python >= 3.9
