# ============================================================
# AI数字人财经直播平台 V4.0 — Jetson Xavier NX 依赖
# ============================================================
# 硬件:     NVIDIA Jetson Xavier NX 8GB
# 系统:     Ubuntu 20.04 LTS
# JetPack:  5.x (L4T R35.x)
# CUDA:     11.4
# TensorRT: 8.x
# Python:   3.8.10 (系统原生)
# CPU:      ARM64 (aarch64)

# ── GPU & ONNX (Jetson 专用 wheel) ──
# 注意: onnxruntime-gpu 需要通过 NVIDIA 官方 wheel 安装
# sudo apt-get install -y python3.8-dev libopenblas-dev
# pip install https://github.com/microsoft/onnxruntime/releases/download/v1.16.3/onnxruntime_gpu-1.16.3-cp38-cp38-linux_aarch64.whl
onnx>=1.14.0,<2.0.0

# ── 计算机视觉 (Jetson 优化版) ──
# opencv 使用 JetPack 内置版本 (OpenCV 4.5.4 + CUDA)
# sudo apt-get install -y libopencv-dev python3-opencv
pillow>=9.0.0,<10.4.0

# ── 音频处理 (精简版) ──
librosa==0.9.2              # 最后支持 Python 3.8 的版本
soundfile>=0.10.0,<0.13.0

# ── 数据源 ──
akshare>=1.10.0,<2.0.0

# ── 科学计算 ──
scipy>=1.5.0,<1.11.0
scikit-learn>=1.0.0,<1.3.0
matplotlib==3.5.3           # 最后支持 3.8 的稳定版

# ── 视频输出 ──
# av 在 ARM64 上需要从源码编译: pip install av --no-binary av

# ── TTS 引擎 ──
piper-tts>=1.2.0,<2.0.0    # 纯 Python + ONNX，ARM64 原生支持

# ── Jetson 系统监控 ──
# jetson-stats (tegrastats 替代，可选)
# sudo pip install jetson-stats
