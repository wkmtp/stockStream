# ============================================================
# AI数字人财经直播平台 V4.0 — Demo 演示依赖
# ============================================================
# 用途:      客户演示 / 快速体验
# 特性:      全模拟数据，零外部依赖
# Python:    3.8+

# ── Demo 专用 ──
# 仅依赖 common.txt + 轻量图形库
pillow>=9.0.0,<10.4.0
matplotlib==3.5.3           # K 线图表渲染

# ── TTS (可选) ──
piper-tts>=1.2.0,<2.0.0    # 文本转语音 (离线)

# ── 数据源 ──
akshare>=1.10.0,<2.0.0     # 可选：真实行情数据源

# 注意: Demo 模式无需 GPU / ONNX / CUDA
# 安装: pip install -r requirements/common.txt -r requirements/demo.txt
